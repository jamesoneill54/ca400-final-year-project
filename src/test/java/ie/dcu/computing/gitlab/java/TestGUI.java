package ie.dcu.computing.gitlab.java;

import org.junit.Assert;
import org.junit.jupiter.api.*;

public class TestGUI {

    /*
    private GUI testGUI = new GUI(300, 300);

    @Test
    void testClass() {
        Assert.assertEquals(300, testGUI.getFrame().getHeight());
        Assert.assertEquals(300, testGUI.getFrame().getWidth());
    }

    Unable to test this due to X11 Display being needed to display the java.awt.Frame object.

     */
}
